# URL pública del servidor backend.
# Usada para generar enlaces absolutos a recursos servidos por Node.js,
# como la vista previa de la tienda. En desarrollo, es típicamente http://localhost:3000.
BACKEND_URL=http://localhost:3000